Requirements:

Un .env avec:

REACT_APP_PREDICTION_API_URL=/image
DANGEROUSLY_DISABLE_HOST_CHECK=true
HOST=localhost

Avoir le docker de l'ia lancé en local sur le port 80 (de base quand on exporte le docker sur custom vision)